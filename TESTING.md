# Lyra 空 Vault 测试清单

> 目的：验证"用户从 Obsidian 商店 / BRAT 把 Lyra 装到一个干净 vault"后，插件能正常加载、核心功能可用。
> 用**独立的空 vault** 测，别污染你的工作 vault。

## 准备

1. 新建一个空文件夹，比如 `E:\test-lyra-vault`（里面什么都不放）
2. Obsidian → 左下角/菜单 → **Open another repository as vault** → 选这个空文件夹
3. 进入空 vault 后：设置 → **第三方插件** → 关闭「安全模式（Restricted mode）」
   （第一次进空 vault 会提示，确认即可）

## 安装插件（二选一）

### 方式 A：BRAT 装（模拟真实用户，推荐）
1. 第三方插件 → 浏览 → 搜 **BRAT**（Better Reading Experience... 不对，是 "BRAT" by TheDevOwen）→ 安装 → 启用
2. 命令面板（Ctrl+P）→ 搜 `BRAT: Search for plugins` → 输入 `workbench-dashboard` → 安装
   - 这会从 GitHub Release 下载 `main.js` + `manifest.json`，**测的就是发布版**
3. 装好后在「已安装插件」里确认 **Lyra** 出现并启用

### 方式 B：商店装（如果已上架可搜到）
1. 第三方插件 → 浏览 → 搜 **Lyra** → 安装 → 启用
2. （刚上架可能有缓存延迟，搜不到就用方式 A）

## 配置（设置 → Lyra）

按默认值即可，或按需填：
- 工作台标题：随便（默认 Lyra）
- 主题：A
- 专注模式：中
- 灵感捕捉存储目录：`0-收件箱`（空 vault 里会自动建）
- 每日/每周笔记目录：`0-收件箱/每日`、`0-收件箱/每周`
- 项目看板目录：`项目文档`
- 番茄钟：25 / 5
- 项目阶段：默认（待办/进行中/已完成）

> 空 vault 没有这些目录，插件应**自动创建**或**优雅处理空状态**（不报错、不白屏）。这是重点测试项。

## 功能逐项测试

对每项标 ✅ 通过 / ❌ 失败 + 现象：

### 基础加载
- [ ] 命令面板（Ctrl+P）搜「打开工作台」→ 能打开工作台视图，**不报错不白屏**
- [ ] 工作台顶栏显示标题、主题、专注模式切换按钮
- [ ] 空 vault 无笔记时，各区域显示空状态文案（不 crash）

### 任务列表（首页）
- [ ] 在空 vault 新建一篇笔记，写一条任务：`- [ ] 测试任务 #待办 📅 2026-09-05`
- [ ] 回工作台 → 「今日」或「未来 7 天」列表出现该任务
- [ ] 列表里**打勾**完成任务 → 任务变 `[x]`，带 `#进行中`/`#待办` 标签的**标签被清掉**（重点）
- [ ] 取消打勾 → 恢复未完成

### 看板
- [ ] 新建一篇 `项目文档/测试项目看板.md`（用命令「新建项目看板（模板）」生成）
- [ ] 工作台「项目」区出现「测试项目看板」
- [ ] 看板按 待办/进行中/已完成 分栏
- [ ] 在看板卡片上**打勾完成** → 进「已完成」+ 阶段标签被清
- [ ] **拖拽**卡片从「进行中」到「已完成」→ `[x]` + 阶段标签被清
- [ ] **拖拽**卡片从「已完成」到「进行中」→ 去 `[x]` + 加回 `#进行中`
- [ ] 通用看板：无项目标签、带 `#进行中` 的任务出现在「进行中」栏

### 笔记卡片墙
- [ ] 在 vault 里建几篇笔记 → 工作台「卡片墙」按目录显示卡片
- [ ] 点卡片 → 跳转到对应笔记

### 灵感捕捉
- [ ] 工作台「灵感」区，输入捕捉一条灵感 → 生成灵感文件
- [ ] 灵感卡片显示
- [ ] 点灵感卡片 → 跳转到灵感文件
- [ ] 点灵感卡片「立项」按钮 → 生成项目看板 + 灵感标记已立项
- [ ] 已立项徽标（🚀 + 项目名）显示在灵感卡片/列表

### 番茄钟
- [ ] 工作台「番茄钟」区，点开始 → 倒计时走
- [ ] 专注结束 → 自动切休息提示
- [ ] 暂停/重置正常

### 主题/专注模式
- [ ] 顶栏切主题 A/B/C → 工作台配色变化
- [ ] 切专注模式（强/中/弱/隐）→ 背景氛围变化

### 持久化（重启验证）
- [ ] 关闭 Obsidian 重开 → 工作台配置（标题/主题/目录）保留
- [ ] 之前创建的任务/看板/灵感仍在（数据在 vault 文件里，不丢）

## 失败记录格式

```
[区域] 现象：
复现步骤：
期望：
```

## 通过标准

全部 ✅ → 发布版 OK。
任何 ❌ → 记录现象，回开发 vault（`Work-Thoughtbank`）复现并修复，再走 发布流程（改 main.js → 升 version → 推 GitHub → 新 Release → 商店更新）。
